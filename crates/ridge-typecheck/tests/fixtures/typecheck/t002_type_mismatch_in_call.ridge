-- expect: T002
-- Passing Text where Int is expected at a call site. The mismatch is reported
-- against the argument that does not fit, naming the callee and the position.
fn add (x: Int) (y: Int) -> Int = x + y
fn bad = add "hello" 2
