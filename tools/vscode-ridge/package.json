{
  "name": "vscode-ridge",
  "displayName": "Ridge",
  "description": "Ridge language support for VS Code: syntax and semantic highlighting, diagnostics, hover, go-to-definition, go-to-type-definition, find-references, rename, signature help, formatting, and completion from ridge-lsp.",
  "version": "0.2.1",
  "publisher": "ridge-lang",
  "icon": "images/icon.png",
  "license": "Apache-2.0",
  "homepage": "https://github.com/ridge-lang/ridge#readme",
  "bugs": {
    "url": "https://github.com/ridge-lang/ridge/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ridge-lang/ridge"
  },
  "galleryBanner": {
    "color": "#1a1a2e",
    "theme": "dark"
  },
  "keywords": [
    "ridge",
    "ridge-lang",
    "beam",
    "erlang",
    "functional"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Programming Languages",
    "Linters"
  ],
  "activationEvents": [
    "onLanguage:ridge"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "ridge",
        "aliases": [
          "Ridge",
          "ridge"
        ],
        "extensions": [
          ".ridge"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "commands": [
      {
        "command": "ridge.run",
        "title": "Run",
        "category": "Ridge"
      },
      {
        "command": "ridge.test",
        "title": "Run Test",
        "category": "Ridge"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "ridge.run",
          "when": "false"
        },
        {
          "command": "ridge.test",
          "when": "false"
        }
      ]
    },
    "configuration": {
      "title": "Ridge",
      "properties": {
        "ridge.lspPath": {
          "type": "string",
          "default": "",
          "description": "Absolute path to the ridge-lsp binary. Leave empty to auto-resolve (tries `~/.cargo/bin/ridge-lsp` first, then walks PATH). Set this when the binary lives in a non-standard location."
        },
        "ridge.codeLens.references": {
          "type": "boolean",
          "default": true,
          "description": "Show a \"N references\" code lens above each referenceable top-level declaration."
        },
        "ridge.codeLens.implementations": {
          "type": "boolean",
          "default": true,
          "description": "Show a \"N implementations\" code lens above each class declaration."
        },
        "ridge.codeLens.run": {
          "type": "boolean",
          "default": true,
          "description": "Show a \"Run\" code lens above the main function of an app or service project."
        },
        "ridge.codeLens.runTest": {
          "type": "boolean",
          "default": true,
          "description": "Show a \"Run test\" code lens above each @test function."
        }
      }
    },
    "grammars": [
      {
        "language": "ridge",
        "scopeName": "source.ridge",
        "path": "./syntaxes/ridge.tmLanguage.json"
      }
    ],
    "semanticTokenTypes": [
      {
        "id": "capability",
        "superType": "modifier",
        "description": "An effect capability annotation (io, fs, net, db, ...)."
      }
    ],
    "semanticTokenScopes": [
      {
        "scopes": {
          "capability": [
            "storage.modifier.capability.ridge",
            "storage.modifier"
          ]
        }
      }
    ],
    "configurationDefaults": {
      "[ridge]": {
        "editor.semanticHighlighting.enabled": true
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "bundle": "esbuild src/extension.ts --bundle --outfile=out/extension.js --external:vscode --format=cjs --platform=node --target=node18",
    "test": "pnpm run bundle && node test/grammar.test.mjs && node test/activation.test.mjs && node test/indentation.test.mjs",
    "vscode:prepublish": "pnpm run bundle"
  },
  "packageManager": "pnpm@11.1.1",
  "dependencies": {
    "vscode-languageclient": "~9.0"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "esbuild-wasm": "^0.21.0",
    "typescript": "^5.3.0",
    "vscode-oniguruma": "^2.0.1",
    "vscode-textmate": "^9.2.0"
  }
}
