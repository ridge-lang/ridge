-- expect: T011
-- An alias stands for something else, and this pair stands for nothing: each
-- resolves to the other and neither reaches a real type. The parser accepts
-- it, so the type checker is the only thing that can say so.
type A = B
type B = A

fn f (x: A) -> Int = 0
