# tools/vscode-ridge-test/apps/vscode-diag-fixture/ridge.toml — project manifest for
# the diagnostics fixture.
#
# The empty `capabilities.allow = []` whitelist makes any function-level
# capability (`io`, `net`, …) trigger R016 CapabilityNotAllowed.

[project]
name    = "vscode-diag-fixture"
version = "0.1.0"
kind    = "library"

[project.exports]
public   = ["Sample.*"]
internal = []

[capabilities]
allow = []
