-- expect: T007
-- Matching an Int scrutinee against a Text literal. The scrutinee sets the
-- expectation and the pattern is what claims to meet it, so the message names
-- them in that order.
fn f (x: Int) -> Int =
    match x
        "hello" -> 1
        _       -> 2
